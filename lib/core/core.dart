/// The Admin app's view of the cross-cutting core: the shared core layer
/// (api, auth, config, utils, widgets) plus this app's generated localizations.
library;

export 'package:dcpl_shared/core/core.dart';

export '../l10n/l10n.dart';
